//const chrome = require('selenium-webdriver/chrome')

process.env.USE_PROMISE_MANAGER = false;

jasmine.DEFAULT_TIMEOUT_INTERVAL = 50520e3;

//let JestScreenshotReporter = require('jest-screenshot-reporter');

//jasmine.getEnv().addReporter(new JestScreenshotReporter({ chrome }));
